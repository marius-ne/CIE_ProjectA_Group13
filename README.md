# CIE_ProjectA_Group13
Code for Project A of Group 13 in CIE.

# Installation

1. Install Python 3.12.2 or use `pyenv local 3.12.2` if you have `pyenv`.
2. Activate a virtual environment. 
3. Install the requirements with `pip install -r requirements.txt`.